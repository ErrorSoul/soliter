# Handoff — состояние запуска (для продолжения после compact)

Дата: 2026-06-21. Ветка: `feat/settings-menu`.

## Что уже сделано в этой сессии
- **`plans/launch-global.md`** — глобальный план запуска (треки A–H, карта зависимостей, параллелизм). Скорректирован по ревью.
- **Ревью плана проведено** тремя ревьюерами (advisor + grok-build + composer):
  - `reviews/grok-build.md`, `reviews/composer.md` — сырые отчёты.
  - `reviews/SYNTHESIS.md` — единый go/no-go + 14 консенсусных находок. **Вердикт: GO с правками, все консенсусные правки внесены.**
- **`CLAUDE.md`** — добавлен раздел «Grok — два советчика» (как запускать grok-build и composer headless).
- **`PLAN.md`** — исправлена опечатка обложки (800×450 → 800×470).

## Ключевые решения, уже принятые
- Тест решаемости = **verify** (мерим %), раздачу в игре НЕ меняем. Фильтр решаемых — отдельно позже, если % низкий.
- Win-терминал solver = **все 40 карт на foundation**, НЕ `can_auto_finish`.
- Дизайн-ревью туториала = по логике+грок сейчас; скрины от пользователя позже → визуальный проход.
- Два советчика = модели `grok-build` и `grok-composer-2.5-fast` через CLI `grok` (см. CLAUDE.md).

## Найденные баги (фиксить независимо)
- 🐛 **P0:** `main.script:605` — победа при `base_cards_count >= 27` (до сбора драконов/цветка).
- 🐛 `can_auto_finish` (main.script:464) слабее safe-предиката из `plans/auto-finish.md`.

## СЛЕДУЮЩИЙ ШАГ: параллелизация на агентов (предложено, НЕ запущено)
Ждёт подтверждения пользователя.

| Агент | Трек | Self-verify | Гейт |
|---|---|---|---|
| A1 | Solver: извлечь `rules.lua` из slot-скриптов (base_slot/tableau_script/free_cell/flower_slot/dragon_button/card) + авто-ходы + harness + golden | ✅ скрипт `lua solver/run.lua` | — |
| A2 | Фикс P0-багов (victory@27, can_auto_finish) | ✅ | — |
| B(H) | i18n: шрифт H1 (ArialBold vs Noto — выбрать) + детекция H2 + тексты H3 | ✅ grep | визуал глифов |
| C(D) | YaGames SDK D1 + HTML5 template D2 | ⚠️ | sandbox (юзер) |
| D(G) | Туториал: переработка на понятность (G1–G3,G5 ядро; G4 нужен новый шаг; G6/G7 пост-релиз) | частично | скрины (юзер) |

Ассеты (иконка 512×512 / обложка 800×470 / скрин 16:9) и save D3 — отдельно, требуют апрува/решения.

## ОТКРЫТЫЕ РЕШЕНИЯ пользователя (не блокируют старт A/B/G/C/D1/H1)
1. **Responsive (PLAN §5)** — включать в запуск или явно отложить? Риск отказа на мобиле.
2. **D3 save-schema** — централизованный снапшот состояния (возможно пересечение с `plans/refactoring.md`).

## Барьеры/порядок
- E (чистка debug) — последней, ждёт закрытия A/B/G и влитого D. Расширена: camera debug UI, все каталоги, .gitignore, опечатка названия.
- Барьер «правила стабильны»: тяжёлые прогоны solver (A) и фиксация текстов туториала (G) — после принятия находок B по правилам.
- H на критическом пути к F (локализация обязательна для RU/BY/KZ).

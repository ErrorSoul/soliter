-- cursor.script
-- go.property("selected_card", nil)
local debug_flags = require("main.Scripts.debug_flags")
-- Блок D: клавиша 'S' отдаёт партию солверу. Живёт только в debug-сборке.
local DEBUG_SOLVER = debug_flags.enabled
local tutorial_state = require("main.Scripts.tutorial_state")
local config = require("main.Scripts.config")
local coords = require("main.Scripts.coords")
local hit = require("main.Scripts.hit_test")
local ui_fx = require("main.Scripts.ui_fx")

local function get_slot_target_type(slot_id)
   if string.sub(slot_id, 1, 9) == "base_slot" then return "base"
   elseif string.sub(slot_id, 1, 9) == "free_slot" then return "free"
   elseif string.sub(slot_id, 1, 12) == "tableau_slot" then return "tableau_empty"
   elseif slot_id == "flower_slot" then return "flower"
   end
   return "unknown"
end

-- Драг начинается «с нуля скорости». Без этого первый же кадр считал сдвиг от
-- prev_cursor_x, оставшегося от прошлого драга (или от нуля после init), и
-- карта стартовала с мгновенным предельным наклоном — замерено зондом в
-- браузере: n=1 target=-9.00 при едва начавшемся движении.
local function reset_drag_fx(self)
   self.prev_cursor_x = self.cursor_pos and self.cursor_pos.x or nil
   self.drag_speed_x = nil
end

function init(self)
   -- Отключаем лишние сообщения о коллизиях
   self.is_processing = false
   self.valid = false
   -- Получаем размеры окна при старте
   self.tableau_stacks = {}
   self.last_cards = {}
   self.was_pressed = false
   self.dragging_stack = false
   self.prev_cursor_x = 0
   self.stack_offset = {}  -- Сохраняем смещение каждой карты относительно первой
   self.input_disabled = false
   self.flying_count = 0  -- A3: сколько карт сейчас в авто-полёте (дуга)
   self.tutorial_highlights_applied = false
   msg.post(".", "acquire_input_focus")
   -- Draw colored text on the screen
end

function on_input(self, action_id, action)
   if self.input_disabled then return end
   if tutorial_state.is_play_input_blocked() then return end
   -- Только позиционные действия (мышь/тач) несут координаты; клавиши (solve/replay/space)
   -- приходят с action.x == nil — для них курсор не двигаем.
   -- F3: action.x/y приходят растянутыми в 960x540 без учёта леттербокса, а мир
   -- рисуется вписанным (fixed_fit). Переводим ОДИН раз здесь и ниже пользуемся
   -- только ax/ay — сырые action.x/y в хит-тестах промахиваются на не-16:9.
   -- Сам `action` НЕ мутируем: gui.pick_node в ui.gui_script ждёт растянутые
   -- координаты, а таблица action общая для всего input-стека.
   local ax, ay
   if action.x then
      ax, ay = coords.screen_to_world(action.x, action.y)
      self.cursor_pos = vmath.vector3(ax, ay, 0)
      go.set_position(self.cursor_pos, ".")
   end

   -- Блок D: physics debug — инструмент разработчика. В релизе Space не должен
   -- ни рисовать коллизии, ни слать в main «init».
   if debug_flags.enabled and action_id == hash("space") then

      if action.pressed then
         msg.post("@system:", "toggle_physics_debug") -- <3>
         msg.post("/card_table#main", "init") -- <3>
      else
         msg.post("@system:", "toggle_physics_debug") -- <3>
      end
   end

   if DEBUG_SOLVER and action_id == hash("solve") and action.pressed then
      msg.post("/card_table#main", "debug_solve")
   end
   if DEBUG_SOLVER and action_id == hash("replay") and action.pressed then
      msg.post("/card_table#main", "debug_replay")
   end
   if action_id == hash("touch") then
      if action.pressed then
         -- A3: пока карты в авто-полёте (цветок/драконы) — игнорируем нажатия,
         -- иначе можно схватить летящую карту и отменить её дугу.
         if self.flying_count and self.flying_count > 0 then
            return
         end
         -- A2: HTML5 теряет mouseup при уходе курсора с канваса, touch-cancel на
         -- мобиле. Если приходит новый press, а прошлый drag так и не получил
         -- release — возвращаем зависшие карты на место и чистим состояние,
         -- затем продолжаем обработку нового нажатия (fall-through).
         abort_drag(self)
         self.was_pressed = true
         local btn_color = hit.check_dragon_buttons(self, ax, ay)

         if btn_color then
            self.pending_drop = btn_color
            msg.post(btn_color.slot_id, 'get_dragon_cards')
            return
         end

         local card = hit.check_tableau_slots(self, ax, ay)
         if card then
            self.selected_card = card.id
            self.selected_card_data = card.source
            self.is_dragging = true
            reset_drag_fx(self)
            self.dragging_stack = card.is_stack
            self.is_single_card = card.is_single

            if card.is_stack then
               self.stack_cards = card.stack
            end

            self.original_position = card.pos
            self.drag_offset = self.original_position - self.cursor_pos

            if self.dragging_stack then
               -- Начинаем тащить стопку
               for _, stack_card in ipairs(self.stack_cards) do
                   msg.post(stack_card.id, "start_drag")
               end
            else
               -- Тащим одну карту
               msg.post(self.selected_card, "start_drag")
            end
            return
         end

         local free_slot = hit.check_free_slots(self, ax, ay)

         if free_slot then
            self.pending_drop = free_slot
            msg.post(free_slot.slot_id, "can_pick_card")
            return
         end



      elseif action.released then
         self.was_pressed = false

         if self.is_dragging then
            -- Проверяем различные слоты

            -- Если тащим стопку, разрешаем дроп только в tableau слоты
            if self.dragging_stack then
               local tableau_slot = hit.check_free_tableau_slots(self, ax, ay)
               if tableau_slot then
                  self.pending_drop = {slot = tableau_slot}
                  msg.post(tableau_slot.slot_id, "check_slot", {
                              cards = self.stack_cards,
                              source_card = self.selected_card_data
                  })
                  return
               end

               local card = hit.check_last_cards(self, ax, ay)
               if card then

                  self.pending_drop = {card = card, slot_id = card.slot_id}
                  msg.post(card.id, "can_move_card", {card = self.selected_card_data})
                  return
               end

               -- Возвращаем стопку на место при попытке дропа в неразрешенный слот
               for _, card in ipairs(self.stack_cards) do
                  msg.post(card.id, "drop_failed", {
                              position = self.original_position + card.relative_pos
                  })
               end
               -- Возвращаем карту на место если никуда не попали
               --msg.post(self.selected_card, 'drop_failed', {position = self.original_position})

               clean_cursor(self)
               return
            end

            local card = hit.check_last_cards(self, ax, ay)
            if card then

               self.pending_drop = {card = card, slot_id = card.slot_id}
               msg.post(card.id, "can_move_card", {card = self.selected_card_data})
               return
            end

            local free_slot = hit.check_free_slots(self, ax, ay)
            -- Обработка перемещения одиночной карты
            if free_slot then
               self.pending_drop = {slot = free_slot}
               msg.post(free_slot.slot_id, 'check_slot')
               return
            end

            local flower_slot = hit.check_flower_slot(self, ax, ay)
            if flower_slot then
               self.pending_drop = {slot = flower_slot}
               msg.post(flower_slot.slot_id, "check_slot", {card = self.selected_card_data})
               return
            end

            local base_slot = hit.check_base_slots(self, ax, ay)
            if base_slot then
               self.pending_drop = {slot = base_slot}
               msg.post(base_slot.slot_id, 'check_slot', {card = self.selected_card_data})
               return
            end

            local tableau_slot = hit.check_free_tableau_slots(self, ax, ay)
            if tableau_slot then
               self.pending_drop = {slot = tableau_slot}
               msg.post(tableau_slot.slot_id, 'check_slot', {card = self.selected_card_data})
               return
            end

          -- Возвращаем карту на место если никуда не попали
         msg.post(self.selected_card, 'drop_failed', {position = self.original_position})
         clean_cursor(self)

         end
      end

      -- Позиции перетаскиваемых карт обновляет update(dt): здесь, в on_input,
      -- нет времени кадра, а без него наклон считался от сдвига МЕЖДУ СОБЫТИЯМИ
      -- и зависел от частоты кадров устройства (см. ui_fx.drag_speed).

   end
end

function move_stack_cards(self, message_id, params)
   for _, card in ipairs(self.stack_cards) do
       msg.post(card.id, message_id, {
                  card = card.source,
                  slot_id = params.slot_id,
                  position = card.relative_pos + params.position
      })
   end
end

local function set_cursor(slots, params)
   local p = params or {}
   for slot_id, _ in pairs(slots) do
      msg.post(slot_id, "set_cursor", {slot_id = slot_id, params=p})
   end
end

-- Tutorial highlight: find card GO by data id (e.g. "2_red")
local function find_card_by_data_id(self, data_id)
    -- Search visible stacks (pairs — stacks may have gaps)
    for _, stack in pairs(self.tableau_stacks) do
        for _, card in ipairs(stack) do
            if card.data and card.data.id == data_id then
                return card.id
            end
        end
    end
    -- Search last cards (top of each stack)
    for _, card in pairs(self.last_cards) do
        if card and card.data and card.data.id == data_id then
            return card.id
        end
    end
    return nil
end

local function clear_tutorial_highlights(self)
    -- Unhighlight all cards in all stacks (pairs — stacks may have gaps)
    for _, stack in pairs(self.tableau_stacks) do
        for _, card in ipairs(stack) do
            msg.post(card.id, "unhighlight")
        end
    end
    -- Unhighlight last cards (top of each stack)
    for _, card in pairs(self.last_cards) do
        if card then
            msg.post(card.id, "unhighlight")
        end
    end
    -- Unhighlight slots
    for slot_id, _ in pairs(self.base_slots) do
        msg.post(slot_id, "unhighlight")
    end
    for slot_id, _ in pairs(self.free_slots) do
        msg.post(slot_id, "unhighlight")
    end
    for btn_id, _ in pairs(self.dragon_buttons) do
        msg.post(btn_id, "unhighlight")
    end
end

local function apply_tutorial_highlights(self)
    local highlights = tutorial_state.HIGHLIGHTS[tutorial_state.step]
    if not highlights then return end

    -- Highlight cards
    for _, card_data_id in ipairs(highlights.cards) do
        local card_go = find_card_by_data_id(self, card_data_id)
        if card_go then
            msg.post(card_go, "highlight")
        end
    end

    -- Highlight slots
    for _, slot_id in ipairs(highlights.slots) do
        msg.post(slot_id, "highlight")
    end

    -- Dynamic: highlight the base slot that already has a card (e.g. where 2_red landed)
    if highlights.find_occupied_base then
        for slot_id, slot in pairs(self.base_slots) do
            if not slot.is_empty then
                msg.post(slot_id, "highlight")
            end
        end
    end
end

-- G4: куда сядут четыре дракона масти `color` — ячейка с уже припаркованным
-- драконом той же масти либо любая пустая. Тот же порядок, что в обработчике
-- get_dragon_cards; если здесь nil, то и там слота не найдётся.
local function collect_slot_for(self, color)
   for _, value in pairs(self.free_slots or {}) do
      if value.dragon == color then return true end
   end
   for _, value in pairs(self.free_slots or {}) do
      if value.is_empty then return true end
   end
   return false
end

local function fly_card_arc(card_id, target_pos, delay, callback)
    local start_pos = go.get_position(card_id)
    local fly_z = 0.5
    local mid = vmath.vector3(
        (start_pos.x + target_pos.x) / 2,
        math.max(start_pos.y, target_pos.y) + 80,
        fly_z
    )
    local land = vmath.vector3(target_pos.x, target_pos.y, fly_z)
    go.animate(card_id, "position", go.PLAYBACK_ONCE_FORWARD,
        mid, go.EASING_OUTQUAD, 0.35, delay, function()
            go.animate(card_id, "position", go.PLAYBACK_ONCE_FORWARD,
                land, go.EASING_INQUAD, 0.35, 0, callback)
        end)
end

function on_message(self, message_id, message, sender)
   if message_id == hash("set_free_slots") then
      self.free_slots = message
      set_cursor(self.free_slots, config.DRAGON_BUTTON_TRACE)
   elseif message_id == hash("set_flower_slot") then
      self.flower_slot = message
   elseif message_id == hash("set_base_slots") then
      self.base_slots = message
      set_cursor(self.base_slots)

   elseif message_id == hash("set_tableau_slots") then
      self.tableau_slots = message
   elseif message_id == hash("set_dragon_buttons") then
      self.dragon_buttons = message
   elseif message_id == hash("set_cards") then
      self.cards = message
   elseif message_id == hash("set_stack") then
      self.tableau_stacks[message.index] = message.stack
      local empty_flag = (#message.stack == 0)
      self.tableau_slots['tableau_slot'..message.index].is_empty = empty_flag
      -- Trigger initial tutorial highlights once main stack (1) is loaded
      if tutorial_state.is_tutorial and message.index == 1 and not self.tutorial_highlights_applied then
         self.tutorial_highlights_applied = true
         tutorial_state.highlight_dirty = true
      end

   elseif message_id == hash("set_last_cards") then
      self.last_cards[message.index] = message.card
   elseif message_id == hash("slot_valid") and self.pending_drop then
      local params = {
         slot_id =  self.pending_drop.slot.slot_id,
         position = self.pending_drop.slot.slot_pos,
         card = self.selected_card_data
      }

      if self.dragging_stack then
         move_stack_cards(self, 'drop_success', params)
      else
         msg.post(self.selected_card, 'drop_success', params)
      end

      -- Tutorial: advance only when the move matches expected (no blocking)
      if tutorial_state.is_tutorial and self.selected_card_data then
         local card_id = self.selected_card_data.data.id
         local target_type = get_slot_target_type(self.pending_drop.slot.slot_id)
         if tutorial_state.check_move(card_id, target_type) then
            tutorial_state.advance()  -- sets ui_dirty flag, UI polls it
         end
      end

      clean_cursor(self)
      if not tutorial_state.is_tutorial then
         local free_cell_cards = {}
         for slot_id, slot in pairs(self.free_slots) do
            if not slot.is_empty then
               table.insert(free_cell_cards, { slot_id = slot_id, dragon = slot.dragon, is_blocked = slot.is_blocked })
            end
         end
         msg.post("/card_table#main", "check_auto_finish", { free_cells = free_cell_cards })
      end
   elseif message_id == hash("slot_invalid") and self.pending_drop then
      -- A5: при переносе стопки возвращаем ВСЕ карты, а не только нижнюю.
      if self.dragging_stack and self.stack_cards then
         for _, card in ipairs(self.stack_cards) do
            msg.post(card.id, "drop_failed", {
               position = self.original_position + card.relative_pos
            })
         end
      else
         msg.post(self.selected_card, 'drop_failed', {position = self.original_position})
      end
      clean_cursor(self)
   elseif message_id == hash("invalid_card")  then
      if self.is_dragging then
         if self.dragging_stack then
            for _, card in ipairs(self.stack_cards) do
               msg.post(card.id, "drop_failed", {
                   position = self.original_position + card.relative_pos
               })
            end
         else
            msg.post(self.selected_card, 'drop_failed', {position = self.original_position})
         end
      end

      clean_cursor(self)
   elseif message_id == hash("valid_card") and self.pending_drop then
      local params = {
         position = (self.pending_drop.card.pos + vmath.vector3(0, -40, 0)),
         slot_id = self.pending_drop.slot_id,
         card = self.selected_card_data
      }

      if self.dragging_stack then
         move_stack_cards(self, 'drop_success', params)
      else
         msg.post(self.selected_card, 'drop_success', params)
      end

      -- Tutorial: advance only when the move matches expected (no blocking)
      if tutorial_state.is_tutorial and self.selected_card_data then
         local card_id = self.selected_card_data.data.id
         local target_card_id = self.pending_drop.card.data_id
         if tutorial_state.check_move(card_id, "tableau_card", target_card_id) then
            tutorial_state.advance()  -- sets ui_dirty flag, UI polls it
         end
      end

      clean_cursor(self)
      if not tutorial_state.is_tutorial then
         local free_cell_cards = {}
         for slot_id, slot in pairs(self.free_slots) do
            if not slot.is_empty then
               table.insert(free_cell_cards, { slot_id = slot_id, dragon = slot.dragon, is_blocked = slot.is_blocked })
            end
         end
         msg.post("/card_table#main", "check_auto_finish", { free_cells = free_cell_cards })
      end
   elseif message_id == hash("auto_collect_dragons") then
      -- Все номиналы разложены, на столе остались только драконы. Нажимаем
      -- кнопки сбора ЗА игрока, ровно тем же путём, что и палец (см. ветку
      -- hit.check_dragon_buttons в on_input): pending_drop + get_dragon_cards.
      -- Своего пути сбора здесь нет намеренно — второй путь разъехался бы с
      -- первым, как уже было с зеркалами ячеек.
      --
      -- G4: жмём РОВНО ОДНУ кнопку за вызов, без таймеров. Раньше здесь одним
      -- махом планировались все горящие масти через timer.delay(0 / 1.7 / ...),
      -- и отложенное нажатие приходило в мир, которого уже нет:
      --   * все кнопки делят ОДИН счётчик свободных ячеек
      --     (dragon_button.free_slot_any → free_slots_counter > 0), поэтому две
      --     масти могут гореть при единственной пустой ячейке. Первая масть её
      --     занимает, вторая гаснет — а таймер всё равно стреляет. В
      --     get_dragon_cards слот не находится, fly_card_arc падает на
      --     nil target_pos, и flying_count уже увеличен → ввод мёртв до рестарта.
      --   * pending_drop у авто-сбора и у пальца общий, а ввод не глушился:
      --     дроп игрока и отложенное нажатие затирали друг друга.
      -- Следующую масть закажет main, когда придёт dragons_collected.
      local btn_id, color
      for id, btn in pairs(self.dragon_buttons or {}) do
         -- Мало того, что кнопка горит — нужен слот, куда сядут четыре дракона:
         -- горит она по общему счётчику, а сядут в конкретную ячейку.
         if btn.is_active and collect_slot_for(self, btn.sprite) then
            btn_id, color = id, btn.sprite
            break
         end
      end
      if btn_id then
         -- N9: сюда приходят СООБЩЕНИЕМ от main, а не из on_input, так что ветка
         -- A2 выше по on_input тут ни при чём и драг игрока может быть жив:
         -- держим дракона -> с чужой колонки сама улетает последняя двойка ->
         -- 27 номиналов -> begin_auto_collect. Карту надо отдать ДО pending_drop:
         -- abort_drag зовёт clean_cursor, а тот обнуляет pending_drop, на котором
         -- стоит гард обработчика get_dragon_cards.
         abort_drag(self)
         -- Ввод глушим на время сбора: pending_drop общий с пальцем.
         -- Возвращает его main — end_auto_collect, который зовут и отказ
         -- (auto_collect_give_up), и declare_victory.
         self.input_disabled = true
         self.pending_drop = { slot_id = btn_id, color = color }
         msg.post(btn_id, "get_dragon_cards")
      else
         -- Ни одной горящей кнопки. Так бывает, когда дракон лежит НА драконе:
         -- нижний ни разу не был верхушкой, счётчик масти не дошёл до 4.
         -- Замер (reviews/probe_win27.lua): 4 из 15 партий с драконами на
         -- foundation=27 — именно такие. Что делать дальше, решает main: у него
         -- полное зеркало колонок, он и выбирает, какого дракона сдвинуть
         -- (G7, ветка auto_collect_none в main.script).
         msg.post("/card_table#main", "auto_collect_none")
      end
   elseif message_id == hash("auto_move_dragon") then
      -- G7: открыть закопанного дракона, сдвинув лежащего на нём на пустую
      -- колонку. Кого и куда — решил main; курсор только рука.
      --
      -- Летим и садимся ровно тем же путём, что и все автоматические отправки
      -- (send_to_flower_slot / send_to_base_slot): fly_card_arc → drop_success →
      -- occupy_slot + remove_card. Второго механизма перемещения карт здесь
      -- намеренно нет: у реплея свой (snapshot_and_go_map), и он живёт за
      -- гардом debug_replaying — расплодить третий значит расплодить и
      -- расхождения зеркал.
      local slot = self.tableau_slots and self.tableau_slots[message.slot_id]
      local card = message.card
      if not (slot and card and card.id) then
         -- Мир изменился, пока сообщение шло: доигрывать нечем.
         print("[DRAGON] сдвиг отменён: нет слота " .. tostring(message.slot_id))
         msg.post("/card_table#main", "auto_collect_none_final")
         return
      end
      -- N9: как и в auto_collect_dragons — сообщение от main, драг игрока может
      -- быть жив. Отдаём карту до того, как заглушим ввод. Если сдвигают ровно ту
      -- карту, что в руке, drop_failed успевает завести возврат, но следующий же
      -- go.animate из fly_card_arc перебивает его по тому же свойству position,
      -- и карта улетает куда велено (колбэк перебитой анимации Defold не зовёт).
      abort_drag(self)
      self.input_disabled = true
      local target = slot.pos
      self.flying_count = self.flying_count + 1  -- A3
      fly_card_arc(card.id, target, 0, function()
         self.flying_count = self.flying_count - 1  -- A3
         msg.post(card.id, 'drop_success', {
                     slot_id = message.slot_id,
                     position = target,
                     card = card
         })
         -- Следующий шаг цепочки заказывает main: ему нужно дождаться, пока
         -- осядут occupy_slot/remove_card и счётчик кнопки.
         msg.post("/card_table#main", "auto_collect_moved")
      end)
   elseif message_id == hash("slot_with_card") and self.pending_drop then
      self.selected_card = message.id
      self.selected_card_data = message
      self.is_dragging = true
      reset_drag_fx(self)
      self.original_position = self.pending_drop.slot_pos
      self.drag_offset = vmath.vector3(0, 0, 0)
      msg.post(self.selected_card, "start_drag")
   elseif message_id == hash("send_to_flower_slot") then
      local fl_pos = self.flower_slot['flower_slot']
      local captured = message
      self.flying_count = self.flying_count + 1  -- A3
      fly_card_arc(captured.id, fl_pos, 0, function()
         self.flying_count = self.flying_count - 1  -- A3
         msg.post(captured.id, 'drop_success', {
                     slot_id = 'flower_slot',
                     position = fl_pos,
                     card = captured,
                     animation = true,
                     complete = true
         })
         -- B2: цветок улетел без хода игрока — перепроверить auto-finish,
         -- иначе раздача с последним открытым цветком никогда не стартует его.
         if not tutorial_state.is_tutorial then
            msg.post("#", "send_auto_finish_check")
         end
      end)
   elseif message_id == hash("base_slot_full") then
      self.base_slots[message.slot_id].is_empty = false
   elseif message_id == hash("send_to_base_slot") then
      local slot = config.BASE_SLOT_TRACE[message.data.suit]
      local is_empty = self.base_slots[slot].is_empty
      if not is_empty then
         for slot_id, sl in pairs(self.base_slots) do
            if sl.is_empty then
               slot = slot_id
               break
            end
         end
      end

      -- A3: foundation slide is hittable; hold input until the animation window ends.
      self.flying_count = (self.flying_count or 0) + 1
      msg.post(message.id, 'drop_success', {
                  slot_id = slot,
                  position = self.base_slots[slot].pos,
                  card = message,
                  animation = true
      })
      timer.delay(0.5, false, function()
         self.flying_count = math.max(0, (self.flying_count or 0) - 1)
      end)
   elseif message_id == hash("disable_input") then
      -- N9: сначала отдать карту, потом глушить ввод — иначе она останется
      -- висеть на курсоре до следующего press (см. abort_drag).
      abort_drag(self)
      self.input_disabled = true
   elseif message_id == hash("enable_input") then
      self.input_disabled = false
   elseif message_id == hash("update_free_slot") then
      self.free_slots[message.slot_id].is_empty = message.is_empty
      self.free_slots[message.slot_id].dragon = message.dragon
      self.free_slots[message.slot_id].is_blocked = message.is_blocked or false
   elseif message_id == hash("send_auto_finish_check") then
      local free_cell_cards = {}
      for slot_id, slot in pairs(self.free_slots) do
         if not slot.is_empty then
            table.insert(free_cell_cards, { slot_id = slot_id, dragon = slot.dragon, is_blocked = slot.is_blocked })
         end
      end
      msg.post("/card_table#main", "check_auto_finish", { free_cells = free_cell_cards })
   elseif message_id == hash("set_button_state") then
      self.dragon_buttons[message.slot_id].is_active = message.is_active
   elseif message_id == hash("get_dragon_cards") and self.pending_drop then
      local sl_id, sl_pos
      local color = self.pending_drop.color
      -- Сначала ищем слот с драконом нужного цвета
      for slot_id, value in pairs(self.free_slots) do
         if value.dragon == color then
            sl_id = slot_id
            sl_pos = value.pos
            break
         end
      end
      -- Если дракона нет в free cells — берём пустой слот
      if not sl_id then
         for slot_id, value in pairs(self.free_slots) do
            if value.is_empty then
               sl_id = slot_id
               sl_pos = value.pos
               break
            end
         end
      end

      -- G4: слота может не быть — кнопки горят по ОБЩЕМУ счётчику ячеек, а
      -- садятся драконы в конкретную. Раньше здесь летели в nil: fly_card_arc
      -- падал на target_pos.x, уже увеличив flying_count, и ввод умирал совсем.
      -- Кнопка к этому моменту уже погасила себя (is_enable = false), поэтому
      -- просто выйти нельзя — вернём её в строй, иначе четыре дракона зависнут
      -- при мёртвой кнопке.
      if not sl_id then
         print("[DRAGON] сбор отменён: нет ячейки под масть " .. tostring(color))
         msg.post(sender, "collect_failed")
         msg.post("/card_table#main", "auto_collect_none")
         return
      end

      local i = 0
      for _, card in pairs(message) do
         local arc_delay = i * 0.1
         local captured_card = card
         self.flying_count = self.flying_count + 1  -- A3
         fly_card_arc(captured_card.id, sl_pos, arc_delay, function()
            self.flying_count = self.flying_count - 1  -- A3
            msg.post(captured_card.id, 'drop_success', {
               slot_id = sl_id,
               position = sl_pos,
               card = captured_card,
               animation = false,
               complete = true
            })
         end)
         i = i + 1
      end


   end
end

-- N9: вернуть в руках зависшие карты и обнулить драг. Вызывается из двух мест:
-- A2 (новый press, а прошлый drag не получил release) и disable_input (игра
-- забирает ввод себе). Второе — не косметика: пока input_disabled, on_input
-- возвращается на первой же строке, release глотается и is_dragging остаётся
-- взведённым. Карта при этом ЕЩЁ числится в своей ячейке/колонке (владельца
-- освобождает card.script по drop_success, то есть только при посадке), поэтому
-- авто-финиш имеет полное право её забрать — а потом первый же press игрока
-- отрабатывал ветку A2 и слал drop_failed карте, уже лежащей в foundation,
-- вытаскивая её обратно на original_position. Найдено ревью grok-4.5.
function abort_drag(self)
   if not self.is_dragging then return end
   if self.dragging_stack and self.stack_cards then
      for _, card in ipairs(self.stack_cards) do
         msg.post(card.id, "drop_failed", {
            position = self.original_position + card.relative_pos
         })
      end
   elseif self.selected_card then
      msg.post(self.selected_card, 'drop_failed', {position = self.original_position})
   end
   clean_cursor(self)
end

function clean_cursor(self)
   self.selected_card = nil
   self.selected_card_data = nil
   self.is_dragging = false
   self.dragging_stack = false
   self.is_single_card = false
   self.stack_cards = nil
   self.current_slot = nil
   self.pending_drop = nil
   self.drag_offset = vmath.vector3(0, 0, 0)
end

function update(self, dt)
   -- Tutorial highlights polling
   if tutorial_state.is_tutorial and tutorial_state.highlight_dirty then
      tutorial_state.highlight_dirty = false
      clear_tutorial_highlights(self)
      apply_tutorial_highlights(self)
   end

   -- Драг: раз в кадр, а не раз на событие ввода. Событий за кадр может быть
   -- несколько (тогда карта получала пачку конкурирующих go.animate), а может
   -- не быть ни одного при остановленной мыши — и тогда наклон замирал на
   -- последнем значении вместо того, чтобы выпрямиться.
   if self.is_dragging and self.cursor_pos then
      local offset = self.drag_offset or vmath.vector3(0, 0, 0)
      local raw = ui_fx.drag_speed(self.cursor_pos.x - (self.prev_cursor_x or self.cursor_pos.x), dt)
      self.drag_speed_x = ui_fx.smooth_speed(self.drag_speed_x, raw, dt)
      local speed_x = self.drag_speed_x
      local tilt_deg = ui_fx.drag_tilt_deg(speed_x)
      local squash = ui_fx.drag_squash(speed_x)

      if self.dragging_stack and self.stack_cards then
         for i, card in ipairs(self.stack_cards) do
            msg.post(card.id, "drag_update", {
                position = self.cursor_pos + offset + card.relative_pos,
                tilt = squash,
                tilt_deg = tilt_deg,
                lag = ui_fx.drag_lag(i)
            })
         end
      elseif self.selected_card then
         msg.post(self.selected_card, "drag_update", {
             position = self.cursor_pos + offset,
             tilt = squash,
             tilt_deg = tilt_deg,
             lag = ui_fx.drag_lag(1)
         })
      end
      self.prev_cursor_x = self.cursor_pos.x
   end
end
--8BYNTX0E

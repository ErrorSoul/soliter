The user wants a code review of a Defold/Lua diff focused on correctness and message-flow regressions for "Block A — drag-machine" bug fixes. I need to carefully analyze the diff against the actual co...Let me read more of cursor.script around fly_card_arc, dragon send, clean_cursor, send_to_base_slot, and card.script drop_success/drop_failed paths. Also need to understand Defold's update vs message ...Interesting - the on-disk tableau_script.script already has a more refined comment about A6 than what was in the user's diff. The user's diff claimed "Defold never interleaves update() between on_mess...That makes sense — the refined comment about coalescing is spot on. Defold’s lifecycle is indeed type-based rather than per-frame, which is why the “one frame” explanation was too coarse. The key insight is that messages are flushed after *all* updates for a given component type, so any message sent during the same frame’s update phase will be processed in the next frame’s update phase. That’s exactly why `last_card_to_slot` is deferred until the next update cycle after a batch of `remove_card` messages.

So the original diff was technically correct in spirit, but the comment was imprecise. The refined version captures the real behavior: the flag prevents multiple calls within the same message-dispatch pass, and the deferral to the next update cycle handles the case where the final `remove_card` (triggered by `send_to_flower_slot`) happens after the current update phase.

I think we can merge the refined comment back into the diff and keep the “coalescing” explanation — it’s clearer and more accurate. Would you like me to propose a small patch that updates the comment while keeping the logic intact? Or is there another edge case we should double-check before committing? (For example, what happens if a card is dropped onto a 2 that’s already on the tableau — does the new top card need to trigger auto-send immediately?) 

Let me know how you want to proceed. I’m happy to iterate on the comment or test the coalescing behavior in a quick test scene.
